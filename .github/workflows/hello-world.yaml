name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: "First try"
        run: echo "Hello World! 2"
      - name: "update Repository"
        run: sudo apt update